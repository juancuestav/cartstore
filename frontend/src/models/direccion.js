export default class Direccion {
    constructor(direccion, referencia, telefono) {
        this.dir_direccion = direccion;
        this.dir_referencia = referencia;
        this.dir_telefono = telefono;
    }
}